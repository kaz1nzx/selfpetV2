import {notFound} from "next/navigation";import {requireMembership} from "@/lib/auth";import {signedMediaUrl} from "@/lib/media";import {updateEmployee} from "@/app/actions/data";
export default async function Funcionario({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<Record<string,string|undefined>>}){const {id}=await params;const q=await searchParams;const {supabase,membership}=await requireMembership();const {data:e}=await supabase.from("employees").select("id,name,phone,email,role,active,photo_path").eq("organization_id",membership.organization_id).eq("id",id).maybeSingle();if(!e)notFound();const url=await signedMediaUrl(supabase,e.photo_path);return <>{q.salvo&&<div className="success">Funcionário atualizado.</div>}{q.erro&&<div className="error">Não foi possível salvar. A foto deve ser PNG, JPG ou WEBP com até 3 MB.</div>}<div className="pet-hero card"><div className="entity-photo large">{url?<img src={url} alt=""/>:<span>👤</span>}</div><div><h1>{e.name}</h1><p className="muted">{e.role||"Sem cargo"} · {e.active?"Ativo":"Inativo"}</p></div></div><form className="card form" action={updateEmployee}><input type="hidden" name="id" value={id}/><div className="field"><label>Trocar foto (PNG, JPG ou WEBP)</label><input name="photo" type="file" accept="image/png,image/jpeg,image/jpg,image/webp"/></div><div className="grid two"><F label="Nome" name="name" value={e.name}/><F label="Cargo" name="role" value={e.role}/><F label="Telefone" name="phone" value={e.phone}/><F label="E-mail" name="email" type="email" value={e.email}/><div className="field"><label>Status</label><select name="active" defaultValue={String(e.active)}><option value="true">Ativo</option><option value="false">Inativo</option></select></div></div><button className="btn">Salvar alterações</button></form></>};function F({label,name,type="text",value}:{label:string;name:string;type?:string;value:any}){return <div className="field"><label>{label}</label><input name={name} type={type} defaultValue={value??""} required={name==="name"}/></div>}
