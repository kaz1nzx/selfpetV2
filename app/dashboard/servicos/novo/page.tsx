import { createService } from "@/app/actions/data";
export default async function NovoServico({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){ const q=await searchParams; return <><div className="page-head"><div><h1>Novo serviço</h1></div></div>{q.erro&&<div className="error">Confira os dados.</div>}<form className="card form" action={createService}><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>Descrição</label><textarea name="description"/></div><div className="field"><label>Preço (R$)</label><input name="price" type="number" step="0.01" min="0" required/></div><button className="btn">Salvar serviço</button></form></>; }
