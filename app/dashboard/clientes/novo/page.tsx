import { createCustomer } from "@/app/actions/data";
export default async function NovoCliente({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){ const q=await searchParams; return <><div className="page-head"><div><h1>Novo cliente</h1><p className="muted">Cadastre o tutor responsável pelo pet.</p></div></div>{q.erro&&<div className="error">Confira os dados e tente novamente.</div>}<form className="card form" action={createCustomer}><div className="grid two"><F label="Nome" name="name" required/><F label="Telefone" name="phone"/><F label="WhatsApp" name="whatsapp"/><F label="Email" name="email" type="email"/><F label="Endereço" name="address"/></div><div className="field"><label>Observações</label><textarea name="notes"/></div><button className="btn">Salvar cliente</button></form></>; }
function F({label,name,type="text",required=false}:{label:string;name:string;type?:string;required?:boolean}){return <div className="field"><label>{label}</label><input name={name} type={type} required={required}/></div>}
